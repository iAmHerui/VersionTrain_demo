server:
  port: 7777

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka/

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/heduomi/remote_config
          username: HeDuoMi
          password: herui_7435!
