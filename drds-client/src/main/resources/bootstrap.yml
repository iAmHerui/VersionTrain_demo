eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8888/eureka/

spring:
  application:
    name: drds-client
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
